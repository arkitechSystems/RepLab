node_modules/
dist/
.env
*.log
.DS_Store
Thumbs.db
nul
.claude/
monetization-plan.html
# iOS build artifacts
client/ios/App/Pods/
client/ios/DerivedData/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# Local-only binary asset directories (videos live on the CDN; not versioned)
replab-videos/
server/VidLib/

# Legacy JSON DB from pre-Postgres era — may contain user data, never commit
server/willfit.json
server/replab.json

# Marketing planning docs (kept on disk, not versioned — large binary Office
# files that don't diff well; live source in a shared drive instead)
_marketing/*.xlsx
_marketing/*.docx
